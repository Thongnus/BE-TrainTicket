package com.example.betickettrain.service;

import com.example.betickettrain.entity.Booking;
import jakarta.transaction.Transactional;
import org.springframework.data.domain.Page;
import org.springframework.data.domain.Pageable;

import java.time.LocalDateTime;
import java.util.List;

public interface RefundPolicyService {
//    @Transactional
//    void requestRefundBooking(Integer bookingId);
//    Page<Booking> getRefundRequests(String search, LocalDateTime fromDate, LocalDateTime toDate, Pageable pageable);

}
